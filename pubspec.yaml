name: my_update_app
description: A simple Flutter app with update system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  package_info_plus: ^4.0.2
  url_launcher: ^6.1.14

flutter:
  uses-material-design: true
